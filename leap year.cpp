#include<stdio.h>
#include<conio.h>
 int main()
 {int a;
printf("\n enter the year in four digits");
scanf("%d",&a);
if (a%4==0)
{
    {printf("the year is leap year");
}
}
else
printf("\n the year is not a leap year");
getch();
return(0);
}
